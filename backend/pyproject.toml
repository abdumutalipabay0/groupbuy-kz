[project]
name = "groupbuy-backend"
version = "0.1.0"
description = "FastAPI backend for GroupBuy hackathon MVP"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.111.0",
    "pydantic>=2.7.0",
    "uvicorn[standard]>=0.30.0"
]

[tool.uv]
package = false
